{
	#
	# Controller namespaces
	# @see Forge::init_routes
	# @see http://mojolicio.us/perldoc/Mojolicious/Guides/Routing#Namespaces
	#
	namespaces => [ 'Forge::Controller' ],

	#
	# Generate random security salt for different purposes.
	# Secret passphrases used for signed cookies and the like.
	# @see http://mojolicio.us/perldoc/Mojolicious#secrets
	#
	secrets => [ 'n_a7hzT25TWcCgU8HHHa' ],

	#
	# Server URL
	# A specified URL [scheme]://[host]:[port]/ on which application can be located
	# Port is optional though.
	#
	server_url => 'http://localhost:3000/',

	#
	# A prefix to plugins
	# No prefix - no plugins
	#
	plugin_prefix => '',

	#
	# A prefix to Embed type of plugins
	# The difference between plugins is that it auto-registers it's helpers
	#
	embeds_prefix => 'Forge::Embed',

}